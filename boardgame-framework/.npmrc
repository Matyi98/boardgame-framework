# Hoist all packages to the root node_modules so the Docker multi-stage builds
# work correctly: they copy /repo/node_modules into each service's runtime image,
# which requires every dependency to be visible at the root.
shamefully-hoist=true
